---
layout: base.liquid
pagination:
  data: collections.posts
  size: 1000
permalink: "/index.html"
---
<h2 class="text-2xl md:text-3xl font-bold mb-6">Recent Posts</h2>
<ul class="space-y-3">
  {% for post in collections.posts %}
    <li>
      <a class="text-xl text-blue-800 hover:underline" href="{{ post.url }}">{{ post.data.title }}</a>
      <span class="text-sm text-gray-600">— {{ post.date | date: "%Y-%m-%d" }}</span>
    </li>
  {% endfor %}
</ul>