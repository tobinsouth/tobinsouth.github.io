{
  "private": true,
  "name": "tobins-blog",
  "scripts": {
    "clean": "rm -rf _site",
    "copy-static": "mkdir -p _site && cp -r index.html robots.txt talk.html zoom.html books.html images style _site/ 2>/dev/null || true",
    "build-blog": "npx @11ty/eleventy --input=blog-src --output=_site/blog",
    "build": "npm run clean && npm run copy-static && npm run build-blog",
    "dev": "npm run copy-static && npx @11ty/eleventy --serve --input=blog-src --output=_site/blog"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.0"
  }
}